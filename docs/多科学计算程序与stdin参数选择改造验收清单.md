# 多科学计算程序与 stdin 参数选择改造验收清单

## 自动化

- [ ] 三个模板 manifest、文件名和 SHA-256 校验通过。
- [ ] 新用户产生三个独立程序目录，exe/dll 不发生交叉覆盖。
- [ ] DCR 无 stdin、无额外命令行参数运行成功。
- [ ] BE_FETD radio 1/2 分别向 stdin 写入 `1\n`/`2\n`。
- [ ] FDEM3D_Frequency_Domain radio 1/2 分别向 stdin 写入 `1\n`/`2\n`。
- [ ] 新程序工作目录始终同时存在两个 `.dat`，所选文件等于上传内容，未选文件等于模板默认内容。
- [ ] 同一用户跨程序严格串行，不同用户仍可并行。
- [ ] 超时、用户取消、管理员终止和非零退出均终止进程树并完成归档。
- [ ] 新程序归档包含两个 `.dat`、mesh、日志、task.json 和嵌套结果目录。
- [ ] 历史 DCR 任务列表、详情和下载保持可用。
- [ ] 前端正式构建成功。

## 正式程序人工验收

- [ ] 提供 BE/FDEM 两个真实 exe、各自 dll、四个默认 `.dat`、版本和 SHA-256。
- [ ] 在目标 Windows 主机确认额外 Intel/GFortran 运行库依赖均满足。
- [ ] 真实程序只读取一次 1/2；无需后续交互输入。
- [ ] cwd、硬编码参数文件、mesh 和 Forward_data 路径符合预期。
- [ ] 五类任务（DCR、BE×2、FDEM×2）均完成结果下载与归档核对。
