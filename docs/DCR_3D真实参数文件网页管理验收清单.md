# DCR_3D 真实参数文件网页管理验收清单

- [ ] `docs/model_DC.dat` 可解析，并满足 `parse(serialize(model)) == model`。
- [ ] 上传真实 `.dat` 后网页正确显示全局参数、4 个材料、3 个源和 9 个 MN。
- [ ] 用户可直接增删改材料、源和 MN，错误准确定位到表格行。
- [ ] 保存后 exe 目录 `model_DC.dat` 可被解析，下载内容与当前 SHA-256 一致。
- [ ] 两标签页使用旧 SHA 保存时返回 stale revision，且前端保留编辑内容。
- [ ] 运行/归档期间保存返回 workspace busy，且不影响运行文件。
- [ ] 提交 DCR 任务时 staging 参数等于当时当前版本。
- [ ] 排队后编辑当前参数不改变已提交任务 staging 和最终归档。
- [ ] 任务成功、失败、取消、超时及重启恢复后，exe 目录恢复最新当前参数。
- [ ] 历史版本列表只包含本人归档，跨用户访问返回 404。
- [ ] 加载历史版本后保存只更新当前工作区，历史归档字节和 hash 不变。
- [ ] 旧占位 JSON 任务仍可下载，但标记为不可加载。
- [ ] 程序同步不会覆盖用户已保存的 DCR 当前参数。
- [ ] Alembic、全量 pytest、Vite build 和本机 Mock E2E 通过。
