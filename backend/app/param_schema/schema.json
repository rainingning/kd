{
  "schema_version": "dcr-model-v1",
  "format": "model_DC.dat",
  "description": "DCR_3D 三维直流电阻率有限元正演真实参数结构",
  "fields": [
    {"name": "boundary_mode", "type": "enum", "options": [1, 2], "required": true, "description": "人工边界：1=Robin，2=零 Dirichlet"},
    {"name": "write_vtk", "type": "bool", "required": true, "description": "是否输出每个源的节点电位 VTK"},
    {"name": "air_domain_ids", "type": "array", "required": true, "description": "COMSOL 空气域 Domain ID"},
    {"name": "materials", "type": "array", "required": true, "description": "材料域电阻率和旋转角"},
    {"name": "sources", "type": "array", "required": true, "description": "AB 供电源及各源 MN 观测"}
  ]
}
